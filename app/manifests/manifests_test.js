'use strict';

describe('primate.manifests module', function() {

  beforeEach(module('primate.manifests'));

  describe('manifests controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var manifestsCtrl = $controller('manifestsCtrl');
      expect(manifestsCtrl).toBeDefined();
    }));

  });
});